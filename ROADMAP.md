# 2026年北京市大学生数智会计创新应用竞赛 - 产品演进与迭代路线图 (ROADMAP)

本文档记录了参赛作品 **DeepSeek-AuditMind（数智审计与舞弊穿透智能体）** 从“功能验证型基座”向“稳定参赛作品”收敛补强的核心路线图与任务清单。

---

## 🎯 迭代优先级路线图

| 优先级 | Issue 编号 | 核心模块 | 目标概述 | 状态 |
| :---: | :---: | :---: | :--- | :---: |
| **P1** | **Issue 1** | 产品主线 | 收敛核心业务主线：聚焦“数智审计与舞弊穿透”单点打透 | ✅ 已完成 |
| **P2** | **Issue 3** | 可靠性 | 区分真实模型与 Mock 模式：修复 LLM 失败静默回退，支持 strict-online | ✅ 已完成 |
| **P3** | **Issue 4** | 评测基准 | 重构基准测试：增加字段级与金额级精确比对，告别关键词模糊包含 | ✅ 已完成 |
| **P4** | **Issue 2** | 数据输入 | 完善真实文件导入：支持 Excel、CSV 会计数据上传解析与字段校验 | ✅ 已完成 |
| **P5** | **Issue 5** | 审计专业度 | 补强证据追溯链：明确区分确定性规则计算、LLM推断与待人工核实事项 | ✅ 已完成 |
| **P6** | **Issue 6** | 比赛现场 | 优化现场演示：提供 Windows 一键启动脚本与防掉线离线演示保障，升级全景指挥舱与 28 真实案例 | ✅ 已完成 (超额交付) |
| **P0** | **Issue 7** | 真实性与闭环 | 解决上传审计闭环、彻底隔离评测答案泄漏与 Beneish 两期真实指标、合规 CORS 与 GitHub Actions CI | ✅ 已完成 (全面闭环) |

---

## 📋 核心 Issue 详案

### Issue 1：[产品] 明确首版主线：数智审计与舞弊穿透
**背景**：当前项目同时包含审计舞弊分析和管理会计本量利分析，功能较丰富，但现场演示容易分散重点。  
**实施目标**：
1. 首版聚焦闭环：数据导入 ➔ 确定性核查（借贷平衡/三单勾稽/Beneish）➔ DeepSeek准则研判 ➔ 证据链输出 ➔ 审计底稿与报告导出；
2. 管理会计插件保留在代码库作为扩展模块，不在首屏主路径分散评委注意力；
3. 首页与侧边栏结构简化，确保 5 分钟内完整流畅演示一个典型案例；
4. 更新 README 明确主线。

### Issue 2：[功能] 增加 Excel、CSV 和 PDF 会计数据导入流程
**背景**：目前案例主要来自内置代码数据。为了体现实际应用价值，需支持用户现场上传常见财务表格。  
**实施目标**：
1. 提供标准模板下载（`data/templates/凭证模板.xlsx`, `银行流水.csv`）；
2. 支持前端直接上传 Excel（总账/明细账/记账凭证）和 CSV（银行流水/发票清单）；
3. 增加文件数据解析器，提供日期、金额格式与必填字段的校验与错误提示；
4. 导入后提供数据表格预览，一键转换为 `AccountingCaseData` 直接进入智能体分析。

### Issue 3：[可靠性] 区分 DeepSeek 在线分析与 Mock 演示模式
**背景**：当前 DeepSeek API 请求失败后会自动回退到 Mock 引擎，可能导致用户误以为结果来自真实模型，影响竞赛现场对系统真实性的判断。  
**实施目标**：
1. 建立三种明确运行模式：
   - `strict-online`（严格在线模式）：API 失败立即显式报错中断，严禁任何静默兜底；
   - `online`（在线优先模式）：优先调用线上大模型，明确提示降级状态与具体异常；
   - `mock`（离线演示模式）：明确标注“模拟推理结果（Demo Mode）”，不消耗 Token；
2. 在前端界面与生成的 PDF/Excel 报告中清晰注明当前运行模式与推理模型版本；
3. 评测基座支持单独针对真实模型与 Mock 模式分别运行评测。

### Issue 4：[评测] 重构基准测试，增加字段级和金额级准确率
**背景**：当前 Precision、Recall、F1 主要通过关键词匹配计算，容易出现“文本包含关键词但结论不准确”的情况。  
**实施目标**：
1. 在 `test_cases.py` 中为 4 个案例建立强类型的 `GroundTruthRiskItem` 标准答案；
2. 重构评分逻辑，考核：
   - 风险类型匹配度（枚举级匹配）；
   - 风险严重度准确率（HIGH / MEDIUM / LOW）；
   - 异常金额误差率（$|预测金额 - 真实金额| / 真实金额 \le 1\%$）；
   - 证据单据索引命中率（凭证号/发票号/银行流水号）；
   - 正常对照组误报率（False Positive Rate）；
3. 生成多维严谨评分卡，输出客观真实的指标数据。

### Issue 5：[审计] 为每条风险结论增加原始数据证据链
**背景**：审计智能体的核心可信度取决于结论能否追溯到原始凭证、合同、发票和银行流水。  
**实施目标**：
1. 在 `RiskFinding` 模型中增加证据链追溯：
   - 来源单据与行号索引；
   - 确定性规则计算结论（如提前 11 天跨期、借贷差额 ¥1,438,053）；
   - DeepSeek 大模型准则解释与法律责任认定；
   - 待注册会计师人工核实事项（标记为“需人工执行外部函证/监盘”）；
2. 在标准 Excel 工作底稿与 PDF 报告中开辟“证据溯源与穿透轨迹”专属展示区。

### Issue 6：[演示] 增加一键启动、案例预置和离线演示模式
**背景**：竞赛现场可能存在网络抖动、API 配额不足或评委环境差异，需保证系统 100% 稳定演示。  
**实施目标**：
1. 提供根目录下 `run.bat`（Windows 一键启动批处理文件），自动检查 Python 环境与依赖；
2. 支持一键切换“在线调用 DeepSeek API”与“离线确定性演示”；
3. 启动前自检关键依赖项、输出目录与预置案例完整性；
4. 确保在无网络极端情况下依然能在 3 分钟内完整演示“案例载入 ➔ 确定性穿透 ➔ 仪表盘可视化 ➔ 审计底稿与报告导出”。

### Issue 7：[Review] 修复上传审计闭环、评测答案泄漏与财务指标真实性问题
**背景**：针对 GitHub 代码审查与竞赛专家评审提出的 6 项严谨性指标进行系统性整改与闭环。  
**实施成果**：
1. **上传到审计全业务闭环**：前端/API 上传的凭证/发票/流水数据经严格字段校验后自动注册进全局案例库，并持久化到本地目录；支持根据生成的 `case_id` 立即执行完整审计、SSE 流式推理及全套 Excel/PDF 底稿导出；对空文件及破损文件实施 HTTP 400 拦截并返回具体字段级诊断清单；
2. **彻底隔离基准标准答案（零 Ground Truth 依赖）**：彻底剔除离线推断对 `ground_truth_findings` 的读取，改由从 Prompt 中的案情描述、确定性三单勾稽比对结果与记账凭证明细自底向上动态推导；对凭证变异事实具有实时敏感性（金额变异则输出金额随之变异）；
3. **Beneish M-Score 指标真实性**：废弃所有硬编码假倍数与假分数；模型严谨要求两期对比财报输入；当对比期数据缺失时显式返回 `is_calculable: False` 及缺失字段清单；
4. **CORS 与安全配置合规化**：根除 `allow_origins=["*"]` 与 `allow_credentials=True` 规范冲突，支持环境变量与本地可信源白名单；
5. **云端持续集成自动化 (CI)**：新增 `.github/workflows/ci.yml`，在 Ubuntu 与 Windows 双平台上全自动运行 pytest 自动化测试矩阵与前端 Vite 生产构建。

